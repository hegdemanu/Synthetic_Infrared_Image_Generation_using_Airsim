

# Create main data directories
mkdir -p data/simulated
mkdir -p data/real

# Add .gitkeep files to track empty directories
touch data/simulated/.gitkeep
touch data/real/.gitkeep

# Create a README.md in each directory explaining its purpose
echo "# Simulated IR Images
This directory contains simulated infrared images generated by the IR simulator.

Directory structure:
- train/: Training images
- val/: Validation images
- test/: Test images" > data/simulated/README.md

echo "# Real IR Images
This directory contains real infrared images captured by thermal sensors.

Directory structure:
- train/: Training images
- val/: Validation images
- test/: Test images" > data/real/README.md

# Create subdirectories for both simulated and real data
mkdir -p data/simulated/{train,val,test}
mkdir -p data/real/{train,val,test}

# Add .gitkeep files to all subdirectories
touch data/simulated/{train,val,test}/.gitkeep
touch data/real/{train,val,test}/.gitkeep
